.DS_Store
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
public
public.zip

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/

# MacOs systems files
**/.DS_Store
resources

# Fludh thesse files from cache git rm --cached .DS_Store
# flush files from everywhere - find . -name .DS_Store -print0 | xargs -0 git rm --ignore-unmatch